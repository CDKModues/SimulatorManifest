<?xml version="1.0" encoding="UTF-8"?>
<Simulator>
  <!--
  Name是在CDK中用于显示的工具链名称，建议具备一定可读性
  -->
  <Name>T-Head Simulator</Name>
  <!--
  ID是工具组件的唯一标识符，不对外显示，全网唯一
  -->
  <ID>t-head-cskysim</ID>
  <!--
  Vendor是提供该第三方工具的厂商名
  -->
  <Vendor>T-Head</Vendor>
  <!--
  URL一般是一个web可访问的网站，用于宣传使用
  -->
  <URL>https://occ.t-head.cn/</URL>
  <!--
  Description用于在CDK中显示对该工具的简单说明
  -->
  <Description>平头哥官方IoT芯片系统软件模拟器工具</Description>
  <!--
  Version节点用于描述每个具体的资源，可以存在多个Value值不同的Version用来描述该资源的多个版本
  -->
  <Version Value="V3.8.2">
    <!--
    ResourceType支持的类型有 tar.gz / zip / git 
    -->
    <ResourceType>tar.gz</ResourceType>
    <!--
    ResourceURL是获取该资源的地址，外网可访问
    -->
    <ResourceURL>https://occ-oss-prod.oss-cn-hangzhou.aliyuncs.com/resource/1356021/1640154586368/xuantie-qemu-win-20211217-0918.tar.gz</ResourceURL>
    <!--
    ResourceValidation用于验证ResourceURL的安全性和有效性；
    如果是tar.gz / zip 类型，这里需要提供MD5值；
      Windows下面计算文件MD5值，可以直接在Windows CMD命令行中输入：
      certutil -hashfile D:\your\file\fullpath\filename.tar.gz MD5
    如果是git仓库，这里需要提供commit的hash值
    -->
    <ResourceValidation>f45adb43b4a4c03bcf64f515bc4a014e</ResourceValidation>
    <!--
    1 EnvPath用来设置环境变量，在CDK内部使用该工具链时，会首先设置该环境变量
    2 这里相对路径是资源包下载完成以后的父目录：
      2.1 如果是git形式，则是git clone完成以后的仓库根目录
      2.2 如果是tar.gz / zip形式，则是直接解压出来以后的顶层目录
    3 如果环境变量需要多个，以;（英文格式的分号）为分隔符
    -->
    <EnvPath>xuantie-qemu-win-20211217-0918</EnvPath>
    <Tool Name="Simulator" Value="cskysim.exe"/>
    <Tool Name="CPF32" Value="cpf.exe"/>
    <Tool Name="CPF64" Value="cpf64.exe"/>
    <Tool Name="UserConfig" Value="xuantie-qemu-win-20211217-0918/soccfg"/>
  </Version>
</Simulator>